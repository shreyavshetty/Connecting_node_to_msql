{
  "name": "nodemscpes",
  "version": "0.0.0",
  "description": "backend using nodejs (linked to mysql)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
